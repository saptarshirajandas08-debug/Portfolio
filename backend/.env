MONGO_URI=mongodb://127.0.0.1:27017/portfolio
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Admin login — used only once, by the seed script, to create your admin account
ADMIN_EMAIL=rajansaptarshi@gmail.com
ADMIN_PASSWORD=change-this-before-seeding

# Secret used to sign admin login tokens — make this a long random string
JWT_SECRET=replace-with-a-long-random-string
